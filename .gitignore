# Godot 4 generated/local files
.godot/
.import/
export.cfg
*.translation

# CI export output (see .github/workflows/deploy.yml) -- anchored to the
# repo root so this doesn't also match scripts/build/, an unrelated source
# directory that happens to share the name.
/build/

# OS
.DS_Store
Thumbs.db
